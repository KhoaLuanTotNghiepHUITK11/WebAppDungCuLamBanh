@model IEnumerable<WebDungCuLamBanh.Models.LoaiDungCuModel>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/AdminLayout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TenLoaiDungCu)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TenLoaiDungCu)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id_LoaiDungCu">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id_LoaiDungCu">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id_LoaiDungCu">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
